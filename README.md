### Branch Brunch Desktop AI ###

Built with electron, using Houndify API for text and speech recognition

electron is installed locally, to start the application run:

```bash
# If haven't installed
npm install
# Start
npm start
```
